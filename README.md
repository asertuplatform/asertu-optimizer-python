# asertu Optimizer Python SDK

Official Python SDK for asertu Optimizer.

> Initial placeholder release used to reserve the package name on PyPI.

## Installation

```bash
pip install asertu-optimizer
```

## Usage

```python
from asertu.optimizer import Optimizer

client = Optimizer(api_key="demo")
print(client.optimize("hello world"))
```
